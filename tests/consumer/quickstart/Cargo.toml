[package]
name = "gatehouse-quickstart"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
gatehouse = { version = "=0.6.0-alpha.2", path = "../../.." }
tokio = { version = "1", features = ["macros", "rt"] }
